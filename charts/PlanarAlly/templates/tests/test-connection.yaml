apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "PlanarAlly.fullname" . }}-test-connection"
  labels:
    {{- include "PlanarAlly.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "PlanarAlly.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
